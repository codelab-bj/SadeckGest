from django.urls import path

"""
from .views import MyContrat, MyClient, MyAgence, MyMachines

urlpatterns = [
    path('contrat/', MyContrat.as_view()),
    path('clients/', MyClient.as_view()),
    path('agences/', MyAgence.as_view()),
    path('machines/', MyMachines.as_view())
]
"""